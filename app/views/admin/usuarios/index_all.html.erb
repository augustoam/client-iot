<% breadcrumb :users_all %>

<div class="row">
  <%= search_form_for @q, url: index_all_admin_users_path do |f| %>
    <div class="col s12">
      <div class="input-search">
        <%= button_tag(type: 'submit', class: "btn-flat btn-search") do %>
          <i class="material-icons icon-search icon-hover">search</i>
        <% end %>
        <%= f.search_field :email_cont, class: "form-control", placeholder: "Pesquisar Usuários" %>
      </div>
    </div>
  <% end %>
</div>

<div class="container-row">
  <b><%= plural_model_name_for(User) %></b>
  <div class="row">
    <table class="table">
      <thead>
        <tr>
          <th><%= User.human_attribute_name(:id) %></th>
          <th><%= User.human_attribute_name(:email) %></th>
          <th><%= User.human_attribute_name(:created_at) %></th>
          <th colspan="1"></th>
        </tr>
      </thead>

      <tbody>
        <% @users.each do |user| %>
            <td><%= user.id %></td>
            <td><%= user.email %></td>
            <td><%= ltime user.created_at %></td>
            <td><%= link_to 'Tokens', admin_user_tokens_notificacao_mobile_path(user) %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <div class="row">
    <%= link_icon_to_back admin_home_admin_index_path %>
  </div>
</div>

<div class="center-align">
  <%= will_paginate @users, renderer: MaterializePagination::Rails %>
</div>
