<% breadcrumb :automacao_grupo_new, @grupo %>
<div class="row">
	<h3><%= action_new(@automacao_grupo) %> <%= singular_model_name_for(AutomacaoGrupo) %></h3>
</div>

<%= render 'form', automacao_grupo: @automacao_grupo %>
