<div class="row">
  <% @componentes.each do |componente| %>
    <div class="container">
      <%= render partial: "componentes/#{componente.controle_componente.descricao.downcase}", locals: {componente: componente} %>
    </div>
  <% end %>
</div>
